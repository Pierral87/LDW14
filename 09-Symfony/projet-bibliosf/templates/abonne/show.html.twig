{% extends 'base.html.twig' %}

{% block title %}Abonne{% endblock %}

{% block body %}
    <h1>Abonne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonne.id }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ abonne.pseudo }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{% if abonne.roles.0 == "ROLE_ADMIN" %}Directeur
                {% elseif abonne.roles.0 == "ROLE_BIBLIO" %}Bibliothécaire
                {% elseif abonne.roles.0 == "ROLE_LECTEUR" %}Lecteur
                {% elseif abonne.roles.0 == "ROLE_USER" %}Utilisateur
                {% endif %}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ abonne.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ abonne.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_abonne_index') }}">back to list</a>

    <a href="{{ path('app_abonne_edit', {'id': abonne.id}) }}">edit</a>

    {{ include('abonne/_delete_form.html.twig') }}
{% endblock %}
